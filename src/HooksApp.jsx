/* Explicación:
1. Componente funcional HooksApp:
- HooksApp es un componente funcional que no recibe propiedades (props).
- Retorna un elemento div que contiene el texto "HooksApp".
- Este componente sirve como punto de entrada o contenedor principal para una aplicación basada en hooks. */



// Componente funcional HooksApp
export const HooksApp = () => {
  // Retorna un elemento div con el texto "HooksApp"
  return (
    <div>HooksApp</div>
  );
};
